/**
 * Fill these in once you've created a free Supabase project (see README.md
 * "Cloud backup setup"), then redeploy. The anon key is meant to be public —
 * Supabase's Row Level Security policy (see supabase/schema.sql) is what
 * actually keeps one user's rows away from another, not secrecy of this key.
 * Leaving these blank keeps the app fully local-only, no login required.
 */
export const SUPABASE_URL = '';
export const SUPABASE_ANON_KEY = '';
